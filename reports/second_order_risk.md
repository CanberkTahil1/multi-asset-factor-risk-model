# Second-order risk: Shepard's closed form, and whether three corrections double-count

Generated by `python -m mafrm.factors.second_order_report`. SPEC.md 6.4, 5.3.2, 5.4.2. W4-P3.

Shepard (2009) derives `[1 - K/T]^-2` for a covariance estimated on **one** window. This pipeline estimates `sigma` and `rho` on two, and SPEC.md 5.3 corrects `rho`'s sampling error alone. So the question a reader will ask -- may the eigenfactor stage's 0.84% and Shepard's 5.14% be added? -- is a question about whether two corrections overlap, and CLAUDE.md failure mode 7 says not to answer it by assertion. This report answers it by simulation on the pipeline's own estimators, against comparisons registered in `experiments.md` rows 175-186 before any of it ran.

## The unit, first

`[1 - K/T]^-2` multiplies a **variance** (Shepard Eq. 13 and 32 as written). MWO's cross-check in SPEC.md 6.4 -- Shepard 'predicts 2.0' at `T = 100, N = 50` against an observed bias statistic of 2.25 -- fixes the reading, because `(1 - 0.5)^-1 = 2.0` on volatility while `(1 - 0.5)^-2 = 4.0`. SPEC.md 6.4's worked table then applies the variance multiplier to volatility and calls the result a 'vol understatement'; that convention ran through every earlier report and is **kept and labelled** rather than silently corrected (W4-P3 ruling 6). No figure below is printed without its unit, and `mafrm.risk.shepard.SecondOrderRisk` refuses to format without one.

## SPEC.md 6.4's worked table, computed from the config

`T_eff = 2 tau / ln 2` at the shipped volatility half-lives; `K = 6` and `N = 13` are read off the panel, and SPEC.md 6.4's nominal `N = 15` is shown so its 13.6% reproduces. **H4's arithmetic**: at the short horizon the factor structure divides the second-order risk by 2.27x at `N = 13` in variance and by 2.65x at `N = 15` -- SPEC.md 6.4's 2.6x is the latter, to rounding. The empirical side of H4 is the simulation below and `experiments.md` rows 189-190, not this table.

| Setting | `p` | half-life | `T_eff` | `p/T_eff` | `[1 - p/T]^-2` | understatement (variance) | understatement (volatility) | SPEC.md 6.4 table convention |
|---|---|---|---|---|---|---|---|---|
| Asset-level sample covariance, panel `N = 13` | 13 | 84d | 242.4 | 0.0536 | 1.1166 | **11.7% (variance)** | **5.7% (volatility)** | 11.7% |
| Asset-level sample covariance, SPEC.md 6.4's nominal `N = 15` | 15 | 84d | 242.4 | 0.0619 | 1.1363 | **13.6% (variance)** | **6.6% (volatility)** | 13.6% |
| 6-factor model | 6 | 84d | 242.4 | 0.0248 | 1.0514 | **5.1% (variance)** | **2.5% (volatility)** | 5.1% |
| Asset-level sample covariance, panel `N = 13` | 13 | 252d | 727.1 | 0.0179 | 1.0367 | **3.7% (variance)** | **1.8% (volatility)** | 3.7% |
| Asset-level sample covariance, SPEC.md 6.4's nominal `N = 15` | 15 | 252d | 727.1 | 0.0206 | 1.0426 | **4.3% (variance)** | **2.1% (volatility)** | 4.3% |
| 6-factor model | 6 | 252d | 727.1 | 0.0083 | 1.0167 | **1.7% (variance)** | **0.8% (volatility)** | 1.7% |

## The decomposition, on the panel's own `F`

Truth: `separated_ewma_covariance` of the 3,889-date factor panel at the shipped half-lives (PSD asserted). Each trial draws that many Gaussian rows and runs the pipeline's `ewma_second_moment` and `correlation_from_covariance`; the statistic is `E[w'Fw / w'F_hat w]` at the minimum-variance `w` of `F_hat`, the population `B^2` of SPEC.md 6.2's family 4. `M = 2,000` trials from `model.seed`, read through `eigenfactor.monte_carlo_trials`. Realised Kish sizes on this window: 242.4 at the volatility half-life against the asymptotic 242.4, and 1440.5 against 1454.2 for correlation (short horizon).

### `short` horizon: `tau_sigma = 84`, `tau_rho = 504`

| Condition | estimator | `B^2 - 1` (variance) | MC s.e. | `B` (volatility) | analytic comparand |
|---|---|---|---|---|---|
| (A) both legs at `tau = 84` -- Shepard's case | `single_window` | **5.064%** | 0.211% | 1.0250 | Shepard Eq. 32 at the volatility window: 5.14% (variance) |
| (B) `sigma` at 84, `rho` at 504 -- SPEC.md 5.1's estimator | `split_window_joint` | **2.036%** | 0.154% | 1.0101 | none in closed form -- the sum of (C) and (D) if disjoint |
| (C) `rho` estimated, `sigma` exact | `correlation_only` | **0.647%** | 0.075% | 1.0032 | Shepard at the correlation window: 0.83% (variance); the eigenfactor stage's leg |
| (D) `sigma` estimated, `rho` exact | `volatility_only` | **1.456%** | 0.146% | 1.0073 | `rho = I` closed form at the panel's min-var Herfindahl: 1.27% (variance), a floor |
| (E) fixed factor axes under (B) -- what a per-factor `B_t^F` sees | `per_factor` | **1.035%** | 0.089% | 1.0052 | Jensen `1 + 2/T_eff`: 0.83% (variance); exact `E[1/X] - 1` for these EWMA weights: 0.827% |

Interaction `(B) - (C) - (D)` = **-0.0682%** of variance (MC s.e. 0.0079%), i.e. **-3.4%** of the joint excess. `rho = I` control at the panel's own variances: (D) measured 1.437% against the closed form 1.271%.

Registered comparisons, adjudicated:

| Row | Registered comparison | measured | MC s.e. | registered band | verdict |
|---|---|---|---|---|---|
| 175 | (A) single-window joint against Shepard Eq. 32 at the volatility window | 5.064% | 0.211% | [4.627%, 5.655%] +- 2 s.e. | **HOLDS** |
| 176 | rho = I control: (D) sigma-only against the derived closed form | 1.437% | 0.141% | [1.144%, 1.399%] +- 2 s.e. | **HOLDS** |
| 177 | (C) rho-only, between Shepard at row 109's T = 1890 and at T_eff = 1454 | 0.647% | 0.075% | [0.638%, 0.830%] +- 2 s.e. | **HOLDS** |
| 178 | (D) sigma-only, at least the rho = I closed form and below Shepard's whole | 1.456% | 0.146% | [1.266%, 5.141%] +- 2 s.e. | **HOLDS** |
| 179 | (B) THE DISJOINTNESS TEST: |interaction| as a share of the joint excess | 3.350% | 0.386% | <= 10% of the joint excess | **HOLDS** |
| 180 | (E) per-factor B^2 - 1 against the Jensen term 1 + 2/T_eff (Shepard at p = 1) | 1.035% | 0.089% | 0.830% +- 2 s.e. | **REFUTED** |

### `long` horizon: `tau_sigma = 252`, `tau_rho = 504`

| Condition | estimator | `B^2 - 1` (variance) | MC s.e. | `B` (volatility) | analytic comparand |
|---|---|---|---|---|---|
| (A) both legs at `tau = 252` -- Shepard's case | `single_window` | **1.753%** | 0.117% | 1.0087 | Shepard Eq. 32 at the volatility window: 1.67% (variance) |
| (B) `sigma` at 252, `rho` at 504 -- SPEC.md 5.1's estimator | `split_window_joint` | **1.152%** | 0.098% | 1.0057 | none in closed form -- the sum of (C) and (D) if disjoint |
| (C) `rho` estimated, `sigma` exact | `correlation_only` | **0.647%** | 0.075% | 1.0032 | Shepard at the correlation window: 0.83% (variance); the eigenfactor stage's leg |
| (D) `sigma` estimated, `rho` exact | `volatility_only` | **0.563%** | 0.084% | 1.0028 | `rho = I` closed form at the panel's min-var Herfindahl: 0.42% (variance), a floor |
| (E) fixed factor axes under (B) -- what a per-factor `B_t^F` sees | `per_factor` | **0.396%** | 0.052% | 1.0020 | Jensen `1 + 2/T_eff`: 0.28% (variance); exact `E[1/X] - 1` for these EWMA weights: 0.275% |

Interaction `(B) - (C) - (D)` = **-0.0572%** of variance (MC s.e. 0.0044%), i.e. **-5.0%** of the joint excess. `rho = I` control at the panel's own variances: (D) measured 0.553% against the closed form 0.422%.

Registered comparisons, adjudicated:

| Row | Registered comparison | measured | MC s.e. | registered band | verdict |
|---|---|---|---|---|---|
| 181 | (A) single-window joint against Shepard Eq. 32 at the volatility window | 1.753% | 0.117% | [1.504%, 1.838%] +- 2 s.e. | **HOLDS** |
| 182 | rho = I control: (D) sigma-only against the derived closed form | 0.553% | 0.081% | [0.380%, 0.465%] +- 2 s.e. | **HOLDS** |
| 183 | (C) rho-only, between Shepard at row 109's T = 1890 and at T_eff = 1454 | 0.647% | 0.075% | [0.638%, 0.830%] +- 2 s.e. | **HOLDS** |
| 184 | (D) sigma-only, at least the rho = I closed form and below Shepard's whole | 0.563% | 0.084% | [0.420%, 1.671%] +- 2 s.e. | **HOLDS** |
| 185 | (B) THE DISJOINTNESS TEST: |interaction| as a share of the joint excess | 4.960% | 0.384% | <= 10% of the joint excess | **HOLDS** |
| 186 | (E) per-factor B^2 - 1 against the Jensen term 1 + 2/T_eff (Shepard at p = 1) | 0.396% | 0.052% | 0.276% +- 2 s.e. | **REFUTED** |

## What this settles

**The legs add.** At both horizons the interaction between the `rho` leg and the `sigma` leg is inside the registered share of the joint excess, so the split-window estimator's second-order bias is, to Monte Carlo precision, the `rho`-only bias plus the `sigma`-only bias. That is the disjointness demonstration SPEC.md 5.4.2 asked for, by simulation, with the mechanism stated: for Gaussian data the sample correlation is independent of the sample variances, and the bias is a quadratic form in the estimation error.

**The three-way accounting, one edge at a time.**

1. **Eigenfactor against the VRA** -- measured in W3-P4 (`reports/volatility_regime.md`, SPEC.md 5.4.2): the VRA absorbs 0.017-0.033% of volatility of the eigenfactor correction, exactly quadratic in `a`. Third-order, and rescalable rather than re-measured.
2. **Shepard against the eigenfactor stage** -- condition (C) is the leg SPEC.md 5.3 corrects, and Shepard at the correlation window is its analytic cross-check (0.83% (variance), the 0.836% W3-P3b published). Condition (D) is what the correlation-space ruling left uncorrected. Because (C) and (D) add, quoting the eigenfactor stage's correction beside the `sigma` leg does not double-count. **What does double-count is Shepard's whole-covariance figure at the volatility window** -- it is not this estimator's bias at all: the split-window joint (B) is 2.04% of variance against Eq. 32's 5.14% (variance) at the short horizon, because `rho` sits on a window six times longer. **0.84% + 5.14% is therefore not a sum anyone should form**: the first term is inside the second, and the second is not the pipeline's.
3. **Shepard against the VRA** -- condition (E). What a fixed factor axis sees of the estimation error is the inverse-variance Jensen term, Shepard at `p = 1`, and it can be computed exactly: `E[1/X] - 1` = **0.827%** of variance for the short window's EWMA weights (0.275% long), against the first-order `1 + 2/T_eff` = 0.830% (variance). The simulation's (E) is 1.03% +- 0.09% -- see the verdict column and `experiments.md` row 191 for why the exact figure is the one to use. That exact term is the entire estimation-error content of SPEC.md 5.4's fitted multiplier, and so the size of the Shepard-VRA overlap: **57% of the `sigma` leg and 41% of the split-window joint**. The rest of the `sigma` leg is optimiser-specific and invisible to a statistic fitted on fixed axes.

## What this does to W4-P2b's queued observation at `tau = 21`

W4-P2b held one observation open for this session (`experiments.md` after row 173): at `tau = 21` Shepard's closed form predicts **23.2% (variance)** understatement while family 2's observed `B - 1` was about 3% of volatility, roughly an eighth. Three things in that comparison are now known to be different objects, and they are separated here rather than left as one ratio.

1. **The unit.** 23.2% (variance) is 11.0% (volatility). Half the eighth is the factor of two between the two readings (6.4.1).
2. **The comparand.** Family 2 is estimation-independent: Shepard's formula describes the optimiser-selected portfolio, and what a fixed random direction sees of the estimation error is the Jensen term of condition (E) -- to first order `2/T_eff`, i.e. 3.3% of variance (1.6% of volatility) at `tau = 21`. That is arithmetic on a formula already in this report, not a measurement: it says about half of family 2's rise at the short end is the inverse-variance bias every fixed direction carries, and the rest is the real panel's departure from Gaussian. The like-for-like comparand, family 4, cannot be read at all on the real panel because its `B` is pinned near 1.33 by SPEC.md 6.2.6's specification error at every `tau` (rows 165-173).
3. **The window mismatch.** At the shipped short horizon the whole-covariance closed form overstates the split-window estimator's bias by 2.5x with `rho` on a window 6x longer than `sigma`'s. At `tau = 21` the window ratio is 24x, so the mismatch is larger, not smaller. **The share is not in hand and is not invented here.** The simulation has not been run at `tau = 21`, and it is registered as a W8-P1 question rather than opened now: scaling the measured `sigma` leg by `T_eff` alone puts it at 5.8% and the split-window joint at about **6.5% of variance** against the closed form's 23.2% (variance) -- a 3.6x overstatement -- but the `sigma` leg is not linear in `1/T_eff` at `K/T_eff` near 0.1, which is exactly what the run has to measure. Registered with that figure as the prediction and +-30% as the band, `experiments.md`, W8-P1 forward registration.

**What is reported as the corrected forecast, and why.**

`reports/bias_statistics.md` prints the closed form beside every family-4 forecast: Eq. 13 for the naive `sample` variant, whose estimator is exactly the one-window whole covariance the formula is derived for, and Eq. 32 at the volatility window for every factor variant, **labelled an upper bound** for the reason in point 2. The measured split-window bias above is the honest estimation-error figure for SPEC.md 5.1's estimator: `B = 1.0101` short / `1.0057` long, against a family-4 `B` near 1.33 on the real panel. The difference is SPEC.md 6.2.6's specification error, and no closed form on `K/T` reaches it.

**Which `T_eff` case this is.** Every `sigma`-leg quantity is a second moment, where W3-P3b measured `2 tau / ln 2` transferring at 1.007x; row 175 checks it here on Shepard's own case. The `rho` leg is the normalised case, where it does not, which is why row 177 predicted (C) below the closed form at 1454 and the eigenfactor stage simulates its own estimator rather than reading a `T` off the formula.

**10 of 12 registered comparisons hold; rows 180, 186 are refuted at the registration and explained at `experiments.md` row 191.** Nothing here is a `model-config` row: no stage changed, nothing was selected, and `config/model.yaml`'s risk keys are untouched.

## The `tau` grid (W8-P1; experiments.md rows 318-326, registered W4-P3 after row 191)

The forward registration written at W4-P3, run: the same simulation at every point of SPEC.md 5.1.3's grid, the truth held at the short horizon's shipped `F`, `tau_rho` at 504 throughout, `M = 2,000`, and the SEED VARIED across grid points (`model.seed` + grid index) so that nine points are nine measurements. Every figure is in VARIANCE (`B^2 - 1`); `B` on volatility is beside it.

**Row 318, the joint at `tau = 21`: HOLDS** -- measured 5.67% +- 0.32% against the registered 6.5% [4.5%, 8.5%]. Shepard's whole-covariance closed form at that volatility window says 23.2%, 4.1x the measured split-window bias.

**The `rho` leg is `tau_sigma`-independent at 9 of 9 grid points** (within 2 MC s.e. of row 177's 0.647%).

| row | `tau_sigma` | `T_eff` asym. (Kish) | `K/T_eff` | seed | joint `B^2-1` (s.e.) | joint `B` | `rho` leg | `sigma` leg | interaction / joint | Shepard whole @ vol window | whole / joint | `rho` leg verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 318 | 21 | 61 (61) | 0.0990 | 20260825 | 5.67% (0.32%) | 1.0280 | 0.647% (0.075%) | 5.08% (0.31%) | -1.0% | 23.2% | 4.09x | HOLDS |
| 319 | 42 | 121 (121) | 0.0495 | 20260826 | 3.18% (0.22%) | 1.0158 | 0.737% (0.079%) | 2.51% (0.21%) | -2.1% | 10.7% | 3.36x | HOLDS |
| 320 | 63 | 182 (182) | 0.0330 | 20260827 | 2.19% (0.17%) | 1.0109 | 0.649% (0.080%) | 1.61% (0.17%) | -3.4% | 6.9% | 3.17x | HOLDS |
| 321 | 84 | 242 (242) | 0.0248 | 20260828 | 2.21% (0.15%) | 1.0110 | 0.712% (0.078%) | 1.58% (0.15%) | -3.5% | 5.1% | 2.33x | HOLDS |
| 322 | 126 | 364 (364) | 0.0165 | 20260829 | 1.37% (0.13%) | 1.0068 | 0.619% (0.077%) | 0.82% (0.12%) | -5.3% | 3.4% | 2.47x | HOLDS |
| 323 | 168 | 485 (485) | 0.0124 | 20260830 | 1.30% (0.11%) | 1.0065 | 0.738% (0.077%) | 0.63% (0.10%) | -5.4% | 2.5% | 1.94x | HOLDS |
| 324 | 252 | 727 (727) | 0.0083 | 20260831 | 0.98% (0.10%) | 1.0049 | 0.609% (0.075%) | 0.43% (0.08%) | -6.5% | 1.7% | 1.71x | HOLDS |
| 325 | 336 | 969 (969) | 0.0062 | 20260832 | 1.01% (0.09%) | 1.0050 | 0.752% (0.076%) | 0.31% (0.07%) | -5.8% | 1.2% | 1.24x | HOLDS |
| 326 | 504 | 1454 (1440) | 0.0041 | 20260833 | 0.89% (0.09%) | 1.0044 | 0.660% (0.078%) | 0.27% (0.06%) | -4.8% | 0.8% | 0.94x | HOLDS |

**What the grid adds to SPEC.md 6.4.2.** The `rho` leg does not move with `tau_sigma` -- it cannot, since `tau_rho` is pinned -- so the whole of the split-window estimator's `tau` dependence is the `sigma` leg, which scales roughly as `1/T_eff` of the volatility window. The whole-covariance closed form at the volatility window over-predicts the estimator's bias by a factor that GROWS as the window shortens, because it charges `rho`'s estimation error at a window it is not estimated on. That ratio is the number the K/T chart (`reports/kt_scaling.md`) needs to read Shepard's curve honestly: on this estimator the curve is a comparand at the correlation window, and an upper bound at the volatility one.

